import { Props } from './MyBookins.interface';
import styles from './MyBookings.module.scss';

const MyBookings = ({ user, bookings }: any) => {
    return (
        <div className={styles.myBookings}>
            pojdo
        </div>
    );
};

export default MyBookings;