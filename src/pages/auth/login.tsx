import { MyPage } from '@/interfaces/page.interface';

const Login: MyPage = (props: any) => {
  return (
    <div>
      <h1>Login</h1>
    </div>
  );
};

export default Login;

Login.Layout = 'Main';
